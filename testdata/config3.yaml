access:
  repo_plugins:
    owner:
      - serv1

  plugins:
    - name: serv1
      endpoint: 41231
      events:
        - "Merge Request Hook"
        - "Issue Hook"
        - "Note Hook"
        - "Push Hook"

    - name:
      endpoint: http://localhost:7000/gitcode-hook
      events:
