access:
  repo_plugins:
    ibforuorg/test1: # 可以指定社区
      - service-name1

  plugins:
    - name: service-name1
      endpoint: http://localhost:7000/gitcode-hook
      events:
        - "Merge Request Hook"
        - "Issue Hook"
        - "Note Hook"
        - "Push Hook"